{
  "$comment": "運用設定。実機接続はここを書き換えるだけで切替できる。デフォルトはハード不要のシミュレータ。",

  "clinicName": "シスポクリニック",
  "port": 8080,
  "host": "0.0.0.0",

  "currency": {
    "code": "JPY",
    "denominations": [10000, 5000, 1000, 500, 100, 50, 10, 5, 1]
  },

  "changeMachine": {
    "$comment": "driver: 'simulator' でハード無し動作 / 'glory-rt' で実機（要: 上位手順仕様書に基づく実装差込）",
    "driver": "simulator",
    "serial": {
      "path": "/dev/ttyUSB0",
      "baudRate": 9600,
      "dataBits": 8,
      "parity": "even",
      "stopBits": 1
    },
    "initialInventory": {
      "10000": 30, "5000": 30, "1000": 100, "500": 80,
      "100": 120, "50": 60, "10": 150, "5": 60, "1": 200
    }
  },

  "printer": {
    "$comment": "POSからのレシート印字は既定OFF。医療の領収書はレセコンが発行し、クレジット利用控えはUA-P10NA本体が発行するため、通常このPOSでの印字は不要。現金の預り/釣銭メモを紙で出したい場合のみ enabled:true にする。",
    "enabled": false,
    "driver": "simulator",
    "serial": { "path": "/dev/ttyUSB1", "baudRate": 38400 },
    "network": { "host": "192.168.0.31", "port": 9100 }
  },

  "cashless": {
    "$comment": "既定は無効(=POSはキャッシュレスに関与しない)。UA-P10NAは単体で完結して運用するため、POSは現金＋自動釣銭機に専念する。POSでガイド/連動をしたくなった場合のみ enabled:true に戻す(その場合 mode 等は docs/キャッシュレス連携.md 参照)。",
    "enabled": false,
    "terminalModel": "UA-P10NA",
    "mode": "semi",
    "showAmountBarcode": true,
    "barcodeSymbology": "code39",
    "$barcodeComment": "showAmountBarcode:true でスタッフ画面に会計金額のバーコードを表示。端末がバーコード金額入力に対応していれば、これを読ませるだけで手打ちゼロ（契約不要）。非対応なら大きく表示された金額を見て手入力する。",
    "link": {
      "$comment": "mode:'auto' 用。driver: 'simulator'(ハード無しデモ) / 'linked-tcp' / 'linked-serial'。実機autoはCAFIS Arch POS連動の契約が前提（docs/キャッシュレス連携.md）。",
      "driver": "simulator",
      "simulateApprovalMs": 2500,
      "serial": { "path": "/dev/ttyUSB2", "baudRate": 9600, "parity": "none" },
      "network": { "host": "192.168.0.40", "port": 5000 }
    }
  },

  "qr": {
    "$comment": "既定は無効(=POSはQRに関与しない)。PayPay等は静的QRの札立てで独立運用(患者がスキャン→スタッフが着金目視)。POSで扱いたい場合のみ enabled:true。",
    "enabled": false,
    "label": "PayPay"
  },

  "recept": {
    "$comment": "レセコン連携。領収書バーコードの金額フォーマットをパーサで吸収する。",
    "barcode": {
      "parser": "default",
      "$parserComment": "default は『数字のみ＝金額(円)』。院内フォーマットに合わせ backend/src/receipt/billParser.js を編集可。"
    }
  },

  "ui": {
    "defaultLanguage": "ja",
    "languages": ["ja", "en", "zh-CN", "ko"],
    "autoReturnSeconds": 20
  }
}
